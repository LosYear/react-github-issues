export const getRepositoryName = (state) => state.repository.repositoryName;
export const getPageSize = (state) => state.pagination.pageSize;
export const getCurrentPage = (state) => state.pagination.currentPage;